import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
  gameListWrap: {
    padding: "0 130px",
  },
  gameListBg: {
    backgroundImage: "url('/images/gamelist-bg.png')",
  },
}));
